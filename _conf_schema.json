{
  "provider_id": {
    "description": "小说创作使用的模型提供商",
    "type": "string",
    "default": "",
    "hint": "留空则使用默认的模型提供商",
    "_special": "select_provider"
  },
  "max_agent_steps": {
    "description": "Agent 最大执行步骤数",
    "type": "int",
    "default": 30,
    "hint": "控制 Agent 在单次创作中的最大执行步骤"
  },
  "tool_call_timeout": {
    "description": "工具调用超时时间（秒）",
    "type": "int",
    "default": 60,
    "hint": "单个工具调用的超时时间"
  },
  "novel_system_prompt": {
    "description": "Agent 创作时使用的系统提示词，留空则使用内置默认提示词",
    "type": "text",
    "default": "",
    "hint": "可自定义写作风格、质量要求等，留空使用内置提示词"
  }
}
