{
  "max_agent_steps": {
    "description": "Agent 最大执行步骤数",
    "type": "int",
    "default": 30,
    "hint": "控制 Agent 在单次创作中的最大执行步骤"
  },
  "tool_call_timeout": {
    "description": "工具调用超时时间（秒）",
    "type": "int",
    "default": 60,
    "hint": "单个工具调用的超时时间"
  },
  "novel_system_prompt": {
    "description": "小说创作系统提示词",
    "type": "text",
    "default": "你是一位专业的小说创作助手。你可以使用提供的工具来管理角色、关系、事件、剧情大纲和章节内容。请根据用户的要求进行创作，并合理使用工具来维护小说的结构化数据。创作时请注意保持故事的连贯性和角色的性格一致性。",
    "hint": "Agent 在创作时使用的系统提示词，可根据需要自定义"
  }
}
